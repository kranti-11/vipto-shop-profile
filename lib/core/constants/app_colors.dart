import 'package:flutter/material.dart';

/// App color palette for Vipto Kirana Store theme
class AppColors {
  AppColors._();

  // Primary Brand Colors (Deep Indian Kirana Green & Marigold)
  static const Color primary = Color(0xFF2E7D32); // Deep Forest Green
  static const Color primaryLight = Color(0xFF4CAF50);
  static const Color primaryDark = Color(0xFF1B5E20);
  static const Color primaryContainer = Color(0xFFE8F5E9);

  // Secondary Accent Colors
  static const Color secondary = Color(0xFFF9A825); // Festive Marigold Yellow
  static const Color secondaryLight = Color(0xFFFFF59D);
  static const Color accentRose = Color(0xFFD32F2F); // Crimson Red for discount badges

  // Surface & Background Colors
  static const Color background = Color(0xFFFAF8F3); // Soft Warm Cream
  static const Color surface = Colors.white;
  static const Color surfaceVariant = Color(0xFFF3F0E6);
  static const Color cardShadow = Color(0x0F000000);

  // Status & Utility Colors (Indian Kirana Badges)
  static const Color statusOpen = Color(0xFF1E7B34);
  static const Color statusOpenBg = Color(0xFFE6F4EA);
  static const Color ratingBg = Color(0xFFFFF3CD);
  static const Color ratingText = Color(0xFFB8860B);

  // Neutral Grays
  static const Color textPrimary = Color(0xFF1F2937);
  static const Color textSecondary = Color(0xFF4B5563);
  static const Color textMuted = Color(0xFF9CA3AF);
  static const Color border = Color(0xFFE5E7EB);
  static const Color divider = Color(0xFFF3F4F6);
}
